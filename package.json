{
  "name": "verisart-client",
  "version": "0.0.4",
  "description": "Library for generating statements in cidoc-crm",
  "author": "Dan Riley <daniel@verisart.com>",
  "contributors": [
    ""
  ],
  "main": "lib/verisart-client.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/verisart/js-client"
  },
  "keywords": [],
  "dependencies": {},
  "devDependencies": {
    "webpack": "^1.9.6",
    "check-es3-syntax-cli": "^0.1.1",
    "rimraf": "^2.3.4"
  },
  "analyze": true,
  "license": "MIT",
  "scripts": {
    "clean": "rimraf lib",
    "build": "webpack --config webpack.config.js",
    "prepublish": "npm run clean && npm run build && check-es3-syntax lib/ --kill --print",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
